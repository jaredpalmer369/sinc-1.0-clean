export const dynamic = "force-dynamic";

export default function PromptsIndexPage() {
  return (
    <main className="mx-auto max-w-3xl p-6">
      <h1 className="text-2xl font-bold mb-2">Prompts Index (Test Page)</h1>
      <p className="text-sm text-gray-600">
        If you can see this, <code>/prompts</code> is rendering. Open a prompt from your Dashboard to view its detail page.
      </p>
    </main>
  );
}
